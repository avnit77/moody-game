// export default function App() {
//   const [{ coffees, eats, sleeps, studies }, dispatch] = useReducer(reducer, { coffees: 0, eats: 0, sleeps: 0, studies: 0 });

//   return (
//     <>
//       <button onClick={() => dispatch(coffee())}>Drink Coffee</button>
//       <button onClick={() => dispatch(eat())}>Eat Food</button>
//       <button onClick={() => dispatch(sleep())}>Take a Nap</button>
//       <button onClick={() => dispatch(study())}>Study</button>
//       <p>{emoji}</p>
//     </>
//   );
// }
